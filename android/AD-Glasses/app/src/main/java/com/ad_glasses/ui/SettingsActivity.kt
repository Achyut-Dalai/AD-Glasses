package com.ad_glasses.ui

/**
 * Compatibility component-name token for inherited settings intents.
 *
 * The manifest redirects this component to the AD Glasses Compose settings
 * surface; the duplicate legacy settings presentation has been retired.
 */
class SettingsActivity private constructor()
