package com.ad_glasses.ui

/**
 * Compatibility component-name token for inherited intents.
 *
 * The manifest maps this name to the AD Glasses Compose navigation redirect;
 * no legacy chat-list presentation is implemented here.
 */
class ChatListActivity private constructor()
