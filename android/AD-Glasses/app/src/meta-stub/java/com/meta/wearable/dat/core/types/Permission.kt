package com.meta.wearable.dat.core.types

/** Compile-only stand-ins used only in the no-Meta-SDK source set. */
enum class Permission {
    CAMERA,
}

enum class PermissionStatus {
    Granted,
    Denied,
}
