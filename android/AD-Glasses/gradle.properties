# Project-wide Gradle settings.
org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8

android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Keep the JDK host-configured so macOS CI and local environments can supply
# their own Java 17+ installation through JAVA_HOME.

# Shared core artifact version pin (used when includeBuild is disabled).
heycyanCoreConnectivityVersion=0.2.0

# Kotlin/Native's default Ivy download repository conflicts with the
# settings-owned dependency policy. Maven Central hosts the same compiler
# distribution and keeps Apple framework builds within the approved repos.
kotlin.native.distribution.downloadFromMaven=true
