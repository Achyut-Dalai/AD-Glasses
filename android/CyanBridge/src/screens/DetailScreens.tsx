import React, {useEffect, useState} from 'react';
import {StyleSheet, Switch, Text, View} from 'react-native';
import Animated, {useAnimatedStyle, useSharedValue, withRepeat, withTiming} from 'react-native-reanimated';
import {Card,Divider,Field,GlassesImage,IconTile,ListRow,PressableScale,PrimaryButton,Screen,SectionTitle,StatusPill,TopBar,useReducedMotion} from '../design/components';
import {Icon, IconName} from '../design/icons';
import {color, motion, radius, space, type} from '../design/tokens';
import {ADNative, CaptureItem, NoteItem, RecordingItem} from '../native/ADNative';
import {useDashboardState} from '../hooks/useDashboardState';
import type {Navigate, RouteEntry} from '../navigation/routes';

function Page({title, back, children}: React.PropsWithChildren<{title: string; back: () => void}>) { return <Screen><TopBar title={title} onBack={back}/>{children}</Screen>; }
function SettingsGroup({title, children}: React.PropsWithChildren<{title: string}>) { return <View><Text style={styles.groupLabel}>{title}</Text><Card style={{marginTop: 7}}>{children}</Card></View>; }
function TaskHero({icon,title,detail}:{icon:IconName;title:string;detail:string}) { return <Card><View style={styles.taskHero}><IconTile name={icon}/><View style={{flex:1}}><Text style={styles.heroCardTitle}>{title}</Text><Text style={styles.meta}>{detail}</Text></View></View></Card>; }
function Metric({label,value}:{label:string;value:string}) { return <View style={styles.metricRow}><Text style={styles.metricLabel}>{label}</Text><Text style={styles.metricValue}>{value}</Text></View>; }

export function SettingsScreen({navigate,back}:{navigate:Navigate;back:()=>void}) {
  const state=useDashboardState(); const status=state.connecting?'Connecting':state.connected?'Connected':'Disconnected';
  return <Page title="Settings" back={back}><PressableScale onPress={()=>navigate('device')}><View style={styles.glassesSettingsCard}><View style={styles.settingsGlasses}><GlassesImage height={62} dimmed={!state.connected}/></View><View style={{flex:1}}><Text style={styles.cardTitle}>{state.connected?state.deviceName:'Glasses'}</Text><Text style={styles.meta}>{status}</Text></View><Icon name="chevron" size={19} stroke={color.grey500}/></View></PressableScale><SettingsGroup title="Privacy & data"><ListRow icon="lock" title="Privacy" detail="Transcripts, redaction and exports" onPress={()=>navigate('privacy')}/><Divider/><ListRow icon="storage" title="Storage" detail="App data and media from the glasses" onPress={()=>navigate('storage')}/></SettingsGroup><SettingsGroup title="General"><ListRow icon="language" title="Language" detail="App language and system locale" onPress={()=>navigate('language')}/><Divider/><ListRow icon="shield" title="Permissions" detail="Camera, microphone, Bluetooth and nearby devices" onPress={()=>navigate('permissions')}/></SettingsGroup><SettingsGroup title="AD Glasses"><ListRow icon="settings" title="Advanced" detail="Diagnostics and system controls" onPress={()=>navigate('advanced')}/><Divider/><ListRow icon="info" title="About AD Glasses" detail="Version and product information" onPress={()=>navigate('about')}/></SettingsGroup></Page>;
}

export function DeviceScreen({navigate,back}:{navigate:Navigate;back:()=>void}) {
  const state=useDashboardState(); const status=state.connecting?'Connecting to glasses':state.connected?state.deviceName:'Glasses disconnected';
  return <Page title="Device" back={back}><Card style={styles.deviceCard}><View style={styles.deviceStage}><GlassesImage height={170} dimmed={!state.connected}/></View><View style={styles.deviceSummary}><View style={[styles.offlineDot,state.connected&&{backgroundColor:color.success}]}/><View style={{flex:1}}><Text style={styles.heroCardTitle}>{status}</Text><Text style={styles.meta}>{state.connected?'The phone brain is available to your glasses.':'Connect when you want the phone brain available to the glasses.'}</Text></View><StatusPill label={state.connected?'CONNECTED':state.connecting?'CONNECTING':'OFFLINE'} tone={state.connected?'success':'neutral'}/></View>{state.connected?<PrimaryButton label="Disconnect" secondary onPress={()=>ADNative.action('disconnect')}/>:<PrimaryButton label="Connect glasses" onPress={()=>navigate('pairing')}/>}</Card>{state.connected?<Card><Metric label="Battery" value={state.batteryPercent!=null?`${state.batteryPercent}%`:'—'}/><Divider inset={0}/><Metric label="Storage" value={state.storageLabel??'—'}/></Card>:null}<View><SectionTitle>Device tools</SectionTitle><Card style={{marginTop:10}}><ListRow icon="sync" title="Sync media" detail="Bring glasses captures into Library" onPress={()=>navigate('sync')}/><Divider/><ListRow icon="firmware" title="Firmware" detail="Updates and recovery" onPress={()=>navigate('firmware')}/><Divider/><ListRow icon="settings" title="Advanced" detail="Connection diagnostics and Android controls" onPress={()=>navigate('advanced')}/></Card></View></Page>;
}

export function PairingScreen({back}:{back:()=>void}) { return <Page title="Connect glasses" back={back}><View style={styles.pairingHero}><ScanVisual/><Text style={styles.pairingTitle}>Find your glasses</Text><Text style={styles.centerBody}>Keep your glasses nearby and ready to pair. Connection state moves around the product; the glasses themselves never turn into a generic Bluetooth icon.</Text></View><PrimaryButton label="Scan for glasses" icon="sync" onPress={()=>ADNative.action('scan')}/><Card><Text style={styles.cardTitle}>Nearby glasses appear in the native pairing sheet</Text><Text style={[styles.meta,{marginTop:5}]}>The hardware scanner remains native during this migration so vendor-specific pairing stays reliable.</Text></Card></Page>; }
function ScanVisual(){const reduced=useReducedMotion();const pulse=useSharedValue(0);useEffect(()=>{if(reduced){pulse.value=0.35;return;}pulse.value=withRepeat(withTiming(1,{duration:motion.hero}),-1,true);},[pulse,reduced]);const outerStyle=useAnimatedStyle(()=>({opacity:0.28+pulse.value*0.22,transform:[{scale:0.94+pulse.value*0.08}]}));const middleStyle=useAnimatedStyle(()=>({opacity:0.44+pulse.value*0.2,transform:[{scale:0.96+pulse.value*0.05}]}));return <View style={styles.scanStage}><Animated.View style={[styles.scanOuter,outerStyle]}/><Animated.View style={[styles.scanMiddle,middleStyle]}/><View style={styles.scanProduct}><GlassesImage height={104}/></View></View>;}

export function SyncScreen({back}:{back:()=>void}) {const state=useDashboardState();return <Page title="Sync" back={back}><TaskHero icon="sync" title={state.syncActive?'Transfer in progress':state.connected?'Sync media':'Glasses offline'} detail={state.syncActive?'New captures are moving to this phone.':state.connected?'Ready to bring captures onto this phone':'Connect glasses to begin'}/><Card><Metric label="Connection" value={state.connected?state.deviceName:'Not connected'}/><Divider inset={0}/><Metric label="Transfer" value="Local Wi-Fi"/><Divider inset={0}/><Metric label="Media" value={state.storageLabel??'Scanned when sync starts'}/></Card><PrimaryButton label={state.syncActive?'Cancel transfer':state.connected?'Start sync':'Connect glasses'} onPress={()=>ADNative.action(state.syncActive?'stopSync':state.connected?'startSync':'openDeviceSetup')}/></Page>;}

export function CapabilityScreen({route,back}:{route:RouteEntry;back:()=>void}) {const name=String(route.params?.capability??'Capability');const capabilityMeta:Record<string,{icon:IconName;kicker:string;summary:string;output:string}>={Translate:{icon:'translate',kicker:'LIVE TRANSLATION',summary:'Hear a conversation through your glasses and get the meaning back without reaching for the phone.',output:'Translated speech'},Soundbites:{icon:'wave',kicker:'AUDIO NOTES',summary:'Capture spoken moments and turn them into concise notes you can revisit later.',output:'Transcript + note'},Timeline:{icon:'timeline',kicker:'VISUAL MEMORY',summary:'Turn visual captures into a searchable sequence of moments.',output:'Visual timeline'},DayNote:{icon:'book',kicker:'DAILY MEMORY',summary:'Distill the moments that matter into a private note for each day.',output:'Private daily note'},Cron:{icon:'repeat',kicker:'SCHEDULED WORK',summary:'Turn spoken requests into recurring or scheduled tasks without making scheduling feel like a separate app.',output:'Scheduled tasks'},Automation:{icon:'bolt',kicker:'ANDROID ACTIONS',summary:'Open apps, navigate and complete supported Android actions from the glasses.',output:'Approved Android action'}};const data=capabilityMeta[name]??capabilityMeta.Automation!;const[enabled,setEnabled]=useState(false);return <Page title={name} back={back}><View style={styles.capabilityHero}><View style={styles.capabilityIcon}><Icon name={data.icon} size={30}/></View><Text style={styles.kicker}>{data.kicker}</Text><Text style={styles.capabilitySummary}>{data.summary}</Text></View><Card><View style={styles.toggleRow}><View style={{flex:1}}><Text style={styles.heroCardTitle}>{enabled?'On':'Off'}</Text><Text style={styles.meta}>{enabled?'Ready when you use it from the glasses or phone.':'Turn it on when you want this capability available.'}</Text></View><Switch value={enabled} onValueChange={value=>{setEnabled(value);ADNative.action('capabilityToggle',{name,enabled:value});}} trackColor={{false:color.grey300,true:color.grey900}} thumbColor={color.white}/></View></Card><View><SectionTitle>What it does</SectionTitle><Card style={{marginTop:10}}><Metric label="Processing" value={name==='Automation'||name==='DayNote'?'On device':'Configured AI'}/><Divider inset={0}/><Metric label="Saved as" value={data.output}/></Card></View></Page>;}

export function RelayScreen({back}:{back:()=>void}){const[url,setUrl]=useState('');const[backend,setBackend]=useState('Gemini');const[saved,setSaved]=useState(false);const save=()=>{ADNative.action('saveRelay',{url,backend});setSaved(true);};return <Page title="Relay" back={back}><View><SectionTitle caption="The remote route used by web and cloud AI">Server</SectionTitle><Card style={{marginTop:10}}><Field value={url} onChangeText={value=>{setUrl(value);setSaved(false);}} placeholder="https://your-relay.example"/></Card></View><View><SectionTitle>Backend</SectionTitle><Card style={{marginTop:10}}><ListRow icon="cloud" title="Gemini" detail="Gemini CLI through your relay" selected={backend==='Gemini'} onPress={()=>{setBackend('Gemini');setSaved(false);}}/><Divider/><ListRow icon="cloud" title="OpenAI / Codex" detail="OpenAI-compatible route" selected={backend==='OpenAI / Codex'} onPress={()=>{setBackend('OpenAI / Codex');setSaved(false);}}/></Card></View><PrimaryButton label={saved?'Saved':'Save relay'} onPress={save}/></Page>;}
export function LocalAIScreen({back}:{back:()=>void}){const[url,setUrl]=useState('');const[model,setModel]=useState('');const[saved,setSaved]=useState(false);const save=()=>{ADNative.action('saveRemoteServer',{url,model,enabled:true});setSaved(true);};return <Page title="Local AI" back={back}><TaskHero icon="computer" title="On this phone" detail="Import a model for fully local inference."/><PrimaryButton label="Import model file" secondary icon="storage" onPress={()=>ADNative.action('openModelImport')}/><View><SectionTitle caption="Ollama, llama.cpp, vLLM or another compatible endpoint">Compatible server</SectionTitle><Card style={{marginTop:10,gap:10}}><Field value={url} onChangeText={value=>{setUrl(value);setSaved(false);}} placeholder="http://192.168.1.50:11434/v1"/><Field value={model} onChangeText={value=>{setModel(value);setSaved(false);}} placeholder="Model name"/><PrimaryButton label={saved?'Saved':'Save server'} onPress={save}/></Card></View></Page>;}
export function AssistantAppsScreen({back}:{back:()=>void}){return <Page title="Assistant apps" back={back}><TaskHero icon="spark" title="Optional app handoff" detail="Use an installed Gemini or ChatGPT app for selected glasses requests."/><SettingsGroup title="Current assistant"><ListRow icon="computer" title="Android assistant" detail="Choose the phone assistant" onPress={()=>ADNative.action('openAssistantSettings')}/><Divider/><ListRow icon="mic" title="Voice handoff" detail="Uses the selected Android assistant when enabled"/><Divider/><ListRow icon="image" title="Image handoff" detail="Accessibility may be needed for advanced handoff"/></SettingsGroup><SettingsGroup title="Advanced handoff"><ListRow icon="settings" title="Choose Android assistant" onPress={()=>ADNative.action('openAssistantSettings')}/><Divider/><ListRow icon="settings" title="Automation bridge" detail="Runtime compatibility path"/><Divider/><ListRow icon="shield" title="Accessibility" onPress={()=>ADNative.action('openAccessibility')}/></SettingsGroup></Page>;}
export function PrivacyScreen({back}:{back:()=>void}){const[redaction,setRedaction]=useState(true);const[confirm,setConfirm]=useState(true);return <Page title="Privacy" back={back}><TaskHero icon="lock" title="Private by default" detail="Keep sensitive context on this phone whenever the capability supports it."/><SettingsGroup title="Memory & transcripts"><Toggle title="Redact sensitive transcript text" detail="Apply privacy filters before long-term storage" value={redaction} onChange={setRedaction}/><Divider/><Toggle title="Ask before protected automation actions" detail="Require confirmation before sensitive Android actions" value={confirm} onChange={setConfirm}/></SettingsGroup></Page>;}
function Toggle({title,detail,value,onChange}:{title:string;detail:string;value:boolean;onChange:(v:boolean)=>void}){return <View style={styles.toggleRow}><View style={{flex:1}}><Text style={styles.cardTitle}>{title}</Text><Text style={styles.meta}>{detail}</Text></View><Switch value={value} onValueChange={onChange} trackColor={{false:color.grey300,true:color.grey900}} thumbColor={color.white}/></View>;}
export function StorageScreen({back}:{back:()=>void}){return <Page title="Storage" back={back}><TaskHero icon="storage" title="On this phone" detail="Media, models, transcripts and memories stay visible here."/><Card><Metric label="Media" value="Managed by Library"/><Divider inset={0}/><Metric label="Models" value="Managed by Local AI"/><Divider inset={0}/><Metric label="App data" value="Private app storage"/></Card><PrimaryButton label="Open Android storage settings" secondary onPress={()=>ADNative.action('openStorageSettings')}/></Page>;}
export function LanguageScreen({back}:{back:()=>void}){const[lang,setLang]=useState('System default');return <Page title="Language" back={back}><SettingsGroup title="App language">{['System default','English','Hindi'].map((name,i)=><React.Fragment key={name}><ListRow icon="language" title={name} selected={lang===name} onPress={()=>setLang(name)}/>{i<2?<Divider/>:null}</React.Fragment>)}</SettingsGroup></Page>;}
export function PermissionsScreen({back}:{back:()=>void}){return <Page title="Permissions" back={back}><TaskHero icon="shield" title="Only what the glasses need" detail="Permission status explains capability; it never replaces the product itself."/><Card><ListRow icon="camera" title="Camera" detail="Used for vision and captures" value="System"/><Divider/><ListRow icon="mic" title="Microphone" detail="Used for voice and recording" value="System"/><Divider/><ListRow icon="glasses" title="Nearby devices" detail="Used to connect to glasses" value="System"/></Card><PrimaryButton label="Open Android app settings" secondary onPress={()=>ADNative.action('openAppSettings')}/></Page>;}
export function AdvancedScreen({navigate,back}:{navigate:Navigate;back:()=>void}){return <Page title="Advanced" back={back}><Card><ListRow icon="glasses" title="Device diagnostics" detail="Connection, sync, firmware and recovery" onPress={()=>navigate('device')}/><Divider/><ListRow icon="settings" title="Android app settings" detail="Permissions, battery and system controls" onPress={()=>ADNative.action('openAppSettings')}/></Card></Page>;}
export function AboutScreen({back}:{back:()=>void}){return <Page title="About" back={back}><View style={styles.aboutHero}><GlassesImage height={160}/><Text style={styles.aboutName}>AD Glasses</Text><Text style={styles.aboutVersion}>Version alpha</Text></View><Text style={styles.aboutStatement}>A personal companion for displayless smart glasses. The glasses handle the interaction; the phone handles intelligence, tools, memory and media behind the scenes.</Text><SettingsGroup title="Product"><Metric label="Primary interaction" value="Voice through glasses"/><Divider inset={0}/><Metric label="Vision" value="Glasses camera"/><Divider inset={0}/><Metric label="Current information" value="Web through relay"/></SettingsGroup></Page>;}
export function FirmwareScreen({back}:{back:()=>void}){const state=useDashboardState();return <Page title="Firmware" back={back}><TaskHero icon="firmware" title="Firmware" detail="No firmware session is active"/><View><SectionTitle>Preflight</SectionTitle><Card style={{marginTop:10}}><CheckRow title="Firmware support" ready={false}/><Divider inset={0}/><CheckRow title="Bluetooth connected" ready={state.connected}/></Card></View><Card><Text style={styles.cardTitle}>Firmware stays guarded.</Text><Text style={[styles.meta,{marginTop:5}]}>Choose validated files only when the connected glasses expose a supported firmware path.</Text></Card><PrimaryButton label="Choose firmware files" secondary onPress={()=>ADNative.action('chooseFirmware')}/></Page>;}
function CheckRow({title,ready}:{title:string;ready:boolean}){return <View style={styles.checkRow}><Icon name={ready?'check':'info'} size={20} stroke={ready?color.success:color.grey500}/><Text style={[styles.cardTitle,{flex:1}]}>{title}</Text><StatusPill label={ready?'READY':'PENDING'} tone={ready?'success':'neutral'}/></View>;}

export function CapturesScreen({back}:{back:()=>void}){const[items,setItems]=useState<CaptureItem[]>([]);const[loading,setLoading]=useState(true);useEffect(()=>{ADNative.captures().then(setItems).finally(()=>setLoading(false));},[]);return <Page title="Captures" back={back}>{loading?<EmptyState icon="image" title="Loading captures" detail="Reading the media already synced from your glasses."/>:items.length===0?<EmptyState icon="image" title="No captures yet" detail="Sync photos and videos from your glasses when you’re ready."/>:<View style={styles.mediaGrid}>{items.map(item=><PressableScale key={`${item.id}`} onPress={()=>ADNative.action('openUri',{uri:item.uri})} style={styles.mediaTile}><View style={styles.mediaPreview}><Icon name={item.isVideo?'video':'image'} size={30} stroke={color.grey500}/>{item.isVideo?<View style={styles.playOverlay}><Icon name="play" size={18} stroke={color.white}/></View>:null}</View><Text style={styles.cardTitle} numberOfLines={1}>{item.displayName}</Text><Text style={styles.meta}>{item.isVideo?'Video from glasses':'Photo from glasses'}</Text></PressableScale>)}</View>}<PrimaryButton label="Sync from glasses" secondary icon="sync" onPress={()=>ADNative.action('startSync')}/></Page>;}
export function RecordingsScreen({back}:{back:()=>void}){const[items,setItems]=useState<RecordingItem[]>([]);const[loading,setLoading]=useState(true);useEffect(()=>{ADNative.recordings().then(setItems).finally(()=>setLoading(false));},[]);return <Page title="Recordings" back={back}>{loading?<EmptyState icon="wave" title="Loading recordings" detail="Reading audio sessions on this phone."/>:items.length===0?<EmptyState icon="wave" title="No recordings yet" detail="Record from Home or start Soundbites through the glasses."/>:items.map(item=><Card key={`${item.id}`}><PressableScale onPress={()=>ADNative.action('openUri',{uri:`file://${item.audioPath}`})}><View style={styles.recordingRow}><View style={styles.playButton}><Icon name="play" size={20}/></View><View style={{flex:1}}><Text style={styles.cardTitle}>{formatDate(item.startedAt)}</Text><Text style={styles.meta}>{formatDuration(item.durationSec)} · {friendlySource(item.captureSource)}</Text></View></View></PressableScale><View style={styles.waveform}>{Array.from({length:24}).map((_,j)=><View key={j} style={[styles.waveBar,{height:6+((j*7)%18)}]}/>)}</View></Card>)}</Page>;}
export function NotesScreen({back}:{back:()=>void}){const[items,setItems]=useState<NoteItem[]>([]);const[loading,setLoading]=useState(true);useEffect(()=>{ADNative.notes().then(setItems).finally(()=>setLoading(false));},[]);return <Page title="Notes & summaries" back={back}>{loading?<EmptyState icon="note" title="Loading notes" detail="Reading your private memory on this phone."/>:items.length===0?<EmptyState icon="note" title="No notes yet" detail="Soundbites, DayNote and transcript-derived notes will appear here."/>:items.map(item=><Card key={`${item.id}`}><Text style={styles.noteDate}>{formatDate(item.createdAt).toUpperCase()}</Text><Text style={styles.noteTitle}>{item.title||'Untitled note'}</Text><Text style={styles.notePreview}>{item.summary}</Text></Card>)}</Page>;}
function EmptyState({icon,title,detail}:{icon:IconName;title:string;detail:string}){return <View style={styles.emptyState}><View style={styles.emptyIcon}><Icon name={icon} size={27}/></View><Text style={styles.emptyTitle}>{title}</Text><Text style={styles.centerBody}>{detail}</Text></View>;}
function formatDate(ms:number){return new Date(ms).toLocaleString(undefined,{month:'short',day:'numeric',hour:'numeric',minute:'2-digit'});}
function formatDuration(seconds:number){const minutes=Math.max(1,Math.round(seconds/60));return `${minutes} min`;}
function friendlySource(source:string){const text=source.replace(/_/g,' ').toLowerCase();return text.charAt(0).toUpperCase()+text.slice(1);}

const styles=StyleSheet.create({glassesSettingsCard:{minHeight:92,borderRadius:radius.card,backgroundColor:color.surface,borderWidth:1,borderColor:color.grey200,paddingHorizontal:space.md,flexDirection:'row',alignItems:'center',gap:14},settingsGlasses:{width:88,height:62,justifyContent:'center'},cardTitle:{...type.cardTitle,color:color.ink},meta:{...type.meta,color:color.grey700},groupLabel:{...type.meta,color:color.grey700,marginLeft:4},deviceCard:{padding:0,overflow:'hidden'},deviceStage:{height:190,backgroundColor:color.grey100,justifyContent:'center',paddingHorizontal:12},deviceSummary:{padding:space.md,flexDirection:'row',gap:10,alignItems:'center'},offlineDot:{width:8,height:8,borderRadius:4,backgroundColor:color.grey500},heroCardTitle:{...type.section,color:color.ink},pairingHero:{alignItems:'center',paddingVertical:space.lg},scanStage:{width:190,height:176,alignItems:'center',justifyContent:'center'},scanOuter:{position:'absolute',width:176,height:176,borderRadius:88,backgroundColor:color.grey200},scanMiddle:{position:'absolute',width:136,height:136,borderRadius:68,backgroundColor:color.grey100},scanProduct:{width:154,height:104,justifyContent:'center'},pairingTitle:{...type.title,color:color.ink,marginTop:space.xl},centerBody:{...type.body,color:color.grey700,textAlign:'center',maxWidth:330,marginTop:7},taskHero:{flexDirection:'row',alignItems:'center',gap:13},metricRow:{minHeight:48,flexDirection:'row',alignItems:'center'},metricLabel:{...type.body,color:color.ink,flex:1},metricValue:{...type.meta,color:color.grey700,maxWidth:'58%',textAlign:'right'},capabilityHero:{paddingTop:space.xs,gap:10},capabilityIcon:{width:64,height:64,borderRadius:20,backgroundColor:color.grey100,alignItems:'center',justifyContent:'center'},kicker:{...type.micro,color:color.grey700,letterSpacing:1.1,marginTop:4},capabilitySummary:{...type.title,color:color.ink,maxWidth:380},toggleRow:{minHeight:64,flexDirection:'row',alignItems:'center',gap:12},aboutHero:{alignItems:'center',paddingVertical:space.lg},aboutName:{...type.display,color:color.ink,marginTop:8},aboutVersion:{...type.meta,color:color.grey700,marginTop:4},aboutStatement:{...type.body,color:color.grey700,fontSize:17,lineHeight:25},checkRow:{minHeight:54,flexDirection:'row',alignItems:'center',gap:10},mediaGrid:{flexDirection:'row',flexWrap:'wrap',gap:10},mediaTile:{width:'48.5%',backgroundColor:color.surface,borderRadius:radius.card,padding:10,borderWidth:1,borderColor:color.grey200},mediaPreview:{height:130,borderRadius:15,backgroundColor:color.grey100,alignItems:'center',justifyContent:'center',marginBottom:10,overflow:'hidden'},playOverlay:{position:'absolute',width:42,height:42,borderRadius:21,backgroundColor:color.ink,alignItems:'center',justifyContent:'center'},recordingRow:{flexDirection:'row',alignItems:'center',gap:12},playButton:{width:44,height:44,borderRadius:22,backgroundColor:color.grey100,alignItems:'center',justifyContent:'center'},waveform:{height:34,flexDirection:'row',alignItems:'center',gap:3,marginTop:14},waveBar:{width:3,borderRadius:2,backgroundColor:color.grey500},noteDate:{...type.micro,color:color.grey500,letterSpacing:1},noteTitle:{...type.title,color:color.ink,marginTop:10},notePreview:{...type.body,color:color.grey700,marginTop:7},emptyState:{minHeight:330,alignItems:'center',justifyContent:'center',paddingHorizontal:20},emptyIcon:{width:62,height:62,borderRadius:20,backgroundColor:color.grey100,alignItems:'center',justifyContent:'center'},emptyTitle:{...type.title,color:color.ink,marginTop:17}});
